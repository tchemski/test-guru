h1= @test.title
p= @question.body

h2 Answers

= link_to 'New answer', new_admin_answer_path(@question)

ol= render @question.answers

= link_to 'Edit', edit_admin_question_path(@question)
= ' | '
= link_to 'Back', admin_test_path(@question.test)
